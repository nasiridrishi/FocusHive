{
  "name": "@focushive/shared",
  "version": "0.0.1",
  "description": "Shared types and utilities for FocusHive",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "lint": "eslint src --ext .ts",
    "clean": "rm -rf dist coverage"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
