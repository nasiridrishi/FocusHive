<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    
    <!-- Console Appender for Development -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %5p [focushive-backend,%X{traceId:-},%X{spanId:-}] --- [%15.15t] %-40.40logger{39} : %m%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- Profile-specific configurations -->
    <springProfile name="!prod">
        <!-- Development profile - console only -->
        <logger name="com.focushive" level="DEBUG"/>
        <logger name="io.micrometer" level="INFO"/>
        
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>
    
    <springProfile name="prod">
        <!-- Production profile - console logging with structured format -->
        <logger name="com.focushive" level="INFO"/>
        <logger name="io.micrometer" level="INFO"/>
        
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>
    
    <springProfile name="test">
        <!-- Test profile - minimal logging -->
        <logger name="com.focushive" level="WARN"/>
        <logger name="org.springframework" level="WARN"/>
        <logger name="io.micrometer" level="WARN"/>
        
        <root level="WARN">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>
    
    <!-- Business metrics and observability logging -->
    <logger name="com.focushive.observability" level="INFO"/>
    <logger name="com.focushive.security" level="INFO"/>
    <logger name="com.focushive.performance" level="INFO"/>

</configuration>